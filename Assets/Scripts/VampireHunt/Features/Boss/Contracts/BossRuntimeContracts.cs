namespace VampireHunt.Boss.Contracts
{
    // Kept as a dedicated Contracts asset for migration tooling and GUID
    // stability. The public interfaces live in BossContracts.cs so generated
    // Unity project files that predate this asset still compile during refresh.
}
