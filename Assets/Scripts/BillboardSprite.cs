using UnityEngine;

public sealed class BillboardSprite : MonoBehaviour
{
    // Legacy compatibility component. Sprite orientation is authored directly
    // in the scene so it stays editable and does not change at runtime.
}
